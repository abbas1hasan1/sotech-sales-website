import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "SoTech | Your Custom Website Pricing",
  description: "Transparent, straightforward pricing for your website project. See exactly what you'll pay upfront - no hidden fees, no surprises.",
  keywords: ["website design", "web development", "Houston", "pricing", "SoTech"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
